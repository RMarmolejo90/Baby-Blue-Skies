const express = require('express');
const app = express();

// Config
const port = 3000

// Import Dependencies

// Create

// Delete (unsubscribe)

app.listen(port, () => { console.log('Server is running on port 3000')});

